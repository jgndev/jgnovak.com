---
const {slug, logo, description, title, date} = Astro.props;
---
<a href={`/portfolio/${slug}`} aria-label={description}>
    <div class="p-2 m-2 w-[360px] flex flex-col items-center justify-center space-y-2 border border-zinc-900/20 dark:border-zinc-700/30 rounded-md shadow-md">
        <div>
            <img class="w-[300px]" src={logo}
                 alt={description}/>
        </div>
        <div>
            <span class="font-bold text-xl uppercase">{title}</span>
        </div>
        <div class="pb-2">
            <span class="font-light text-sm">{date}</span>
        </div>
    </div>
</a>